import ContactClient from './ContactClient';

export const metadata = {
  title: 'Contact AfriFoundry',
  description: 'Get in touch with AfriFoundry. For partnerships, media, investors, or just to talk about building in Africa.',
};

export default function ContactPage() {
  return <ContactClient />;
}
